{
  "name": "normandy-nextgen-study-example",
  "version": "0.1.0",
  "description": "Demonstrates how to make a 2019-style Normandy add-on study",
  "private": true,
  "license": "MPLv2",
  "scripts": {
    "build": "web-ext build -s extension --overwrite-dest",
    "clean": "rimraf web-ext-artifacts",
    "watch": "web-ext run -s extension"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "web-ext": "^2.6.0"
  },
  "dependencies": {
  }
}
